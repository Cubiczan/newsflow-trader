import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/sonner";
import { QueryProvider } from "@/lib/agent/client";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "NewsFlow Trader — Alpaca AI Trading Agents",
  description: "Autonomous LLM-driven news trading agent on Alpaca's Trading API. Built for the Alpaca AI Trading Agents Hackathon (Aug 28 – Sep 4, 2026).",
  keywords: ["Alpaca", "AI trading agent", "LLM trading", "hackathon", "Next.js", "z-ai-web-dev-sdk"],
  authors: [{ name: "NewsFlow Trader" }],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        <QueryProvider>
          {children}
          <Toaster richColors position="top-right" />
        </QueryProvider>
      </body>
    </html>
  );
}
